services:
  minio1:
    image: minio/minio:RELEASE.2025-09-07T16-13-09Z-cpuv1
    container_name: minio
    environment:
      - MINIO_ROOT_USER=${MINIO_ROOT_USER:?MINIO_ROOT_USER is required}
      - MINIO_ROOT_PASSWORD=${MINIO_ROOT_PASSWORD:?MINIO_ROOT_PASSWORD is required}
      - MINIO_DISTRIBUTED_MODE_ENABLED=no

      - MINIO_SCHEME=http
      # - MINIO_DISTRIBUTED_NODES=minio1,minio2,minio3,minio4
      #- MINIO_SKIP_CLIENT=yes
    # volumes:
    #   - minio1-data:/bitnami/minio/data
    ports:
      - "${MINIO_API_EXTERNAL_PORT:-9002}:9000"
      - "${MINIO_CONSOLE_EXTERNAL_PORT:-9001}:9001"
      
    # command: server --address ":9000" --console-address ":9001" http://minio{1...4}/bitnami/minio/data
    command: server --address ":9000" --console-address ":9001" /bitnami/minio/data
    #cpus: "2.0"

  minio-init:
    image: minio/mc
    container_name: minio_init
    depends_on:
      - minio1
    entrypoint: >
      /bin/sh -c "
      until (/usr/bin/mc alias set local http://minio1:9000 ${MINIO_ROOT_USER} ${MINIO_ROOT_PASSWORD}) do echo 'Waiting for MinIO...' && sleep 1; done;
      /usr/bin/mc mb --ignore-existing local/datalake;
      /usr/bin/mc policy set public local/datalake;
      exit 0;
      "